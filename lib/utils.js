/**
 * @module utils
 */

'use strict';


/**
 * Converts the given buffer into a data URI
 * @param {Buffer} data
 * @param {String} type
 */
module.exports.toDataURI = function(data, type) {
  return data;
};
